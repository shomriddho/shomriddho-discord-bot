{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun x tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@shomriddho-discord-bot/db": "workspace:*",
    "@shomriddho-discord-bot/env": "workspace:*",
    "discord.js": "^14.15.3",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "pidusage": "^4.0.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@shomriddho-discord-bot/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.20.0-beta.3",
    "typescript": "catalog:"
  }
}
